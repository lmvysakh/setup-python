name: Licensed

on:
  push:
    branches:
      - test_787_all
  pull_request:
    branches:
      - main

jobs:
  call-licensed:
    name: Licensed
    uses: actions/reusable-workflows/.github/workflows/licensed.yml@main
